import { MenuIcon, MenuWrapper } from "./Menu.style";

const Menu = () => {
  return (
    <MenuWrapper>
      <MenuIcon src={"/icons/hamburger-menu.svg"} />
    </MenuWrapper>
  );
};

export default Menu;
